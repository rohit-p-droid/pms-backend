services:
  - type: web
    name: backend
    runtime: node
    runtimeVersion: 22.22.0
    packageManager: bun
    buildCommand: bun install; bunx tsc
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
