services:
  - type: web
    name: backend
    runtime: node
    runtimeVersion: 22.22.0
    packageManager: bun
    buildCommand: bun install && bun run build
    startCommand: bun run start
    envVars:
      - key: NODE_ENV
        value: production
